把写实 3D 模型放到这里，命名为 model.glb：
  character/model.glb
然后双击根目录的 启动网站.bat，用浏览器打开 http://localhost:8000/，
"关于我"卡片里的 3D 人物就会自动换成你的写实模型。

格式要求：GLB（glTF 2.0 二进制）最佳；不要超过 ~50MB。
来源建议：Sketchfab 搜索 Realistic + Female，筛选 CC0 / CC-BY 许可后下载 GLB；
或直接发模型文件给 Codex 内嵌进页面（无需服务器）。
